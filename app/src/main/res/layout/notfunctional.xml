<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/popupImage"
        android:layout_width="0dp"
        android:layout_height="486dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="120dp"
        android:layout_marginEnd="32dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/popup" />

    <TextView
        android:id="@+id/notFunctionalText"
        android:layout_width="280dp"
        android:layout_height="80dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="144dp"
        android:layout_marginEnd="32dp"
        android:text="Our team are still working on this page, sorry for the inconvenience"
        app:layout_constraintEnd_toEndOf="@+id/popupImage"
        app:layout_constraintStart_toStartOf="@id/popupImage"
        app:layout_constraintTop_toTopOf="@id/popupImage" />

</androidx.constraintlayout.widget.ConstraintLayout>